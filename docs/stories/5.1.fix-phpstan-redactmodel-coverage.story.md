# Story 5.1: Fix PHPStan Type Coverage — RedactModelAttributesAction

**Status**: ready-for-dev  
**Module**: Activity (independent .git)  
**PHPStan errors**: 1  
**Effort**: trivial (add const types)  
**Owned by**: TBD (needs coordination)

## Error

**File**: `app/Actions/RedactModelAttributesAction.php:18`  
**Type**: `typeCoverage.constantTypeCoverage`  
**Message**: Out of 148 possible constant types, only 145 - 97.9 % actually have it. Add more constant types to get over 99 %

## Root Cause

Constants in class missing explicit type declarations (`const TYPE type = value;` instead of `const TYPE = value;`).  
PHPStan L10 requires 99%+ constant type coverage.

## Solution

1. Locate all `const` declarations in `RedactModelAttributesAction.php`
2. Add explicit types: `const string NAME = 'value'` instead of `const NAME = 'value'`
3. Verify with `phpstan analyse Modules/Activity --no-progress`
4. Run phpmd + pest (coverage UP)
5. Git sync (fetch/merge/commit/push)

## Files Modified

- `app/Actions/RedactModelAttributesAction.php`

## Success Criteria

- PHPStan: 0 errors on `Modules/Activity`
- PHPMD: no violations
- Pest: all pass, coverage maintained/improved
- Git: commit + push to laraxot/dev

## Notes

- No User model mutation expected (constant types only)
- Standalone action, no cross-module deps
