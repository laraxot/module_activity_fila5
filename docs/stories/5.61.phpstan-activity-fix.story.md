# BMAD Story — Activity PHPStan Fix

## Understand
- **Modulo**: Activity
- **Errori iniziali**: 240
- **Tipologia**: mixed cast, method calls su mixed
- **Regola**: `mixed` -> tipi concreti; cast sicuri

## Plan
1. Analizzare errori per tipo
2. Categorizzare: cast, method calls
3. Sistematizzare fix
4. Verificare con phpstan + phpmd + phpinsights + pest
5. Git sync

## Implement
### Step 1: Analisi
```bash
./vendor/bin/phpstan analyse Modules/Activity --error-format=table 2>&1 | head -100
```

### Step 2: Common Patterns
```php
// Cast su mixed - usare narrowing
if (is_numeric($value)) {
    $int = (int) $value;
}

// Method call su mixed - instanceof check
if ($obj instanceof SomeClass) {
    $obj->method();
}
```

## Verify
- [ ] `./vendor/bin/phpstan analyse Modules/Activity` -> < 10 errori
- [ ] `./tools/phpmd.sh Modules/Activity/app`
- [ ] `./vendor/bin/pest --filter="Activity"`

## Status
- [ ] Da iniziare
